$PACKAGE_NAME
import java.util.Optional;
import java.util.List;
$IMPORT_ENTITY

public interface ${NAME}Service {
    Optional<${NAME}Entity> getById(Long id);
    ${NAME}Entity save(${NAME}Entity entity);
    List<${NAME}Entity> getAll();

}